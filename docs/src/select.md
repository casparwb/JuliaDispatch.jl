# The `JuliaDispatch.Select` Module

This module contains functionality for locating and extracting patches and data at certain locations within the domain. 

```@autodocs
Modules = [JuliaDispatch.Select]
Private = false
```